

const config = {
  iceServers: [{ urls: "stun:stun.l.google.com:19302" }]
};

document.getElementById('idButtonVideoCall').addEventListener('click',function (e) {
  
  document.getElementById('idDialogVideo').open = true;

});
