<header.app-top-app-bar.background>
  <div #headerStart>
    <md-icon-button href=@{DataR $ DoctorSpecialtiesR did} aria-label=_{MsgBack}>
      <md-icon>arrow_back
          
    <h1.title-large>
      _{MsgSpecialty}
      
<main>

  <form method=post action=@{DataR $ DoctorSpecialtiesR did} enctype=#{et} novalidate>
    ^{fw}
    <div.app-form-actions>
      <md-text-button type=button href=@{DataR $ DoctorSpecialtiesR did}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}
