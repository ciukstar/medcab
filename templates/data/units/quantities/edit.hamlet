<header.app-top-app-bar.background>
  <div #headerStart>
    <md-icon-button href=@{DataR $ QuantityR qid} aria-label=_{MsgBack}>
      <md-icon>arrow_back
          
    <h1.title-large>
      _{MsgQuantity}
      
<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR $ QuantityR qid} enctype=#{et} novalidate>
    ^{fw}
    <div.app-form-actions>
      <md-text-button type=button href=@{DataR $ QuantityR qid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
