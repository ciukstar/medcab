<header.app-top-app-bar.background>
  <div #headerStart>
    <md-icon-button href=@{DataR UnitsR} aria-label=_{MsgBack}>
      <md-icon>arrow_back
          
    <h1.title-large>
      _{MsgMeasurementUnit}
      
<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR UnitsR} enctype=#{et} novalidate>
    ^{fw}
    <div.app-form-actions>
      <md-text-button type=button href=@{DataR UnitsR}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
