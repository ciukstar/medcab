<header.app-top-app-bar.background>
  <div #headerStart>
    <md-icon-button href=@{DataR $ SignTagsR ps} aria-label=_{MsgBack}>
      <md-icon>arrow_back
          
    <h1.title-large>
      _{MsgTag}
      
<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR $ SignTagsR ps} enctype=#{et} novalidate>
    ^{fw}
    <div.app-form-actions>
      <md-text-button type=button href=@{DataR $ SignTagsR ps}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
