<header.app-top-app-bar.background>
  <div #headerStart>
    <md-icon-button href=@{DataR $ MedSignR sid} aria-label=_{MsgBack}>
      <md-icon>arrow_back
          
    <h1.title-large>
      _{MsgMedicalSign}
      
<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR $ MedSignR sid} enctype=#{et} novalidate>
    ^{fw}
    <div.app-form-actions>
      <md-text-button type=button href=@{DataR $ MedSignR sid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
